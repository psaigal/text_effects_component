var newComponent = (function(){
	'use strict';

	//When user submits text into text box and hits submit, first a new TextEffect object is created.
	//The appropriate function will be called depending on what button was clicked

	function TextEffects(text) {
		this.text = text;
	}

	return {
	}
})();